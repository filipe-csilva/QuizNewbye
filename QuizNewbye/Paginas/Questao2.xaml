<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="QuizNewbye.Paginas.Questao2"
             Title="Pergunta 2">
    <VerticalStackLayout Margin="20">
        <Label 
            x:Name="LBLPergunta"
            Text="Quantos dedos tem faltando na mão esquerda de um ladrão?"
            FontSize="Large"
            TextColor="#2B0B98"
            FontAttributes="Bold"
            Margin="0,50"
            HorizontalTextAlignment="Center"
            VerticalOptions="Center" 
            HorizontalOptions="Center" />

        <Border StrokeShape="RoundRectangle 20" Padding="20" Background="GhostWhite">
            <VerticalStackLayout>

        <RadioButton
            Content="5 Dedos"
            Value="errado"
            TextColor="Black"
            FontAttributes="Italic"
            CheckedChanged="Verificar"/>
        
        <RadioButton
            Content="3 Dedos"
            Value="errado"
            TextColor="Black"
            FontAttributes="Italic"
            CheckedChanged="Verificar"/>
        
        <RadioButton
            Content="4 Dedos"
            Value="certo"
            TextColor="Black"
            FontAttributes="Italic"
            CheckedChanged="Verificar"/>
        
        <RadioButton
            Content="1 Dedos"
            Value="errado"
            TextColor="Black"
            FontAttributes="Italic"
            CheckedChanged="Verificar"/>
            
            </VerticalStackLayout>
        </Border>

        <Button 
            x:Name="BTNVerificar"
            Margin="0,50"
            HorizontalOptions="Center"
            Text="Verificar resposta"
            Clicked="Button_Clicked"/>

    </VerticalStackLayout>
</ContentPage>